# Rust artifacts
target/

# Node artifacts
build/
prebuilds/
node_modules/

# Swift artifacts
.build/

# Go artifacts
_obj/

# Python artifacts
.venv/
dist/
*.egg-info
*.whl

# C artifacts
*.a
*.so
*.so.*
*.dylib
*.dll
*.pc

# Example dirs
/examples/*/

# Grammar volatiles
*.wasm
*.obj
*.o

# Archives
*.tar.gz
*.tgz
*.zip

# Project-local dev doc (not part of the grammar package)
CLAUDE.md

# tree_sitter/ headers are committed; generated parser artifacts are not —
# `tree-sitter generate` runs via the npm `prepare` script and in CI.
src/parser.c
src/grammar.json
src/node-types.json
