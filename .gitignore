# Rust artifacts
target/

# Node artifacts
build/
prebuilds/
node_modules/

# Swift artifacts
.build/

# Go artifacts
_obj/

# Python artifacts
.venv/
dist/
*.egg-info
*.whl

# C artifacts
*.a
*.so
*.so.*
*.dylib
*.dll
*.pc

# Example dirs
/examples/*/

# Grammar volatiles
*.wasm
*.obj
*.o

# Archives
*.tar.gz
*.tgz
*.zip

# Local nvim-treesitter queries/<lang>/ symlinks (rtp dev override)
queries/gnuplot/

# Cargo.lock: library crate — not committed (release bumps Cargo.toml, not the
# lock, so a tracked lock goes stale and trips cargo publish's dirty check).
/Cargo.lock
